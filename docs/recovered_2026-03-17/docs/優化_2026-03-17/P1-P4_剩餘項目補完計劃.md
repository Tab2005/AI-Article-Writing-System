# P1~P4 剩餘優化補完實作計劃

> 修復日期：2026-03-17
> 優先級：補齊審查報告中漏掉的高工時項目
> 部署環境：Zeabur (PostgreSQL)

---

## 補完項目清單

1. **[P2-6] 拆分龐大的 `KalpaPage.tsx`**：將 1100+ 行的代碼拆分為子組件 (TiandaoPanel, NodeTable等)。
2. **[P2-2] AI Service 重構**：建立統一的 JSON 解析器，並精簡核心生成函數。
3. **[P2-3] 前端 API 型別安全**：移除 `any`，定義精確的請求與回應 interface。
4. **[P1-6] ORM Model 關聯加強**：補齊 `ForeignKey` 與 `relationship` 定義。

---

## 實作細節

### 1. 前端組件拆分 (Kalpa)
- 建立 `src/pages/kalpa/` 目錄。
- 抽取邏輯至獨立 Hook `useKalpaData.ts`。

### 2. AI 解析優化
- 透過 `re.search` 與遞歸清理確保 JSON 解析的魯棒性。

### 3. ORM 關聯
- 使用 `ondelete="CASCADE"` 確保資料一致性。

---

## 驗證方式
- 前端：`npm run build`
- 後端：`pytest` (如果環境支援) 或手動導入測試。
